{
  "name": "atif-visualizer",
  "displayName": "ATIF Preview",
  "description": "Visualize Agent Trajectory Interchange Format (ATIF) files",
  "version": "0.8.0",
  "publisher": "waldek",
  "license": "MIT",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/waldekmastykarz/vsc-atifviz.git"
  },
  "engines": {
    "vscode": "^1.120.0"
  },
  "categories": [
    "Visualization"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "atifPreview",
        "displayName": "ATIF Trajectory Preview",
        "priority": "option",
        "selector": [
          {
            "filenamePattern": "*.json"
          }
        ]
      }
    ],
    "commands": [
      {
        "command": "atif-visualizer.preview",
        "title": "ATIF: Preview Trajectory",
        "icon": "$(type-hierarchy-sub)"
      },
      {
        "command": "atif-visualizer.showSource",
        "title": "ATIF: Show Source",
        "icon": "$(file-code)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "when": "resourceLangId == json && activeCustomEditorId != atifPreview",
          "command": "atif-visualizer.preview",
          "group": "navigation"
        },
        {
          "when": "activeCustomEditorId == atifPreview",
          "command": "atif-visualizer.showSource",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "ATIF Preview",
      "properties": {
        "atifviz.showUtilitySteps": {
          "type": "boolean",
          "default": true,
          "description": "Show utility steps in the trajectory preview by default."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^26.0.1",
    "@types/vscode": "^1.120.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0",
    "eslint": "^10.5.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.0"
  },
  "dependencies": {
    "@vscode/codicons": "^0.0.45"
  }
}
